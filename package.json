{
  "name": "storekit",
  "version": "0.3.1",
  "private": true,
  "type": "module",
  "description": "Painel + ETL + MCP + RAG multi-loja Shopify numa imagem Docker.",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/main.js",
    "test": "node --test test/*.test.js",
    "genkeys": "node scripts/genkeys.mjs",
    "lint": "eslint . || true"
  },
  "dependencies": {
    "better-sqlite3-multiple-ciphers": "^11.6.0",
    "fastify": "^5.1.0",
    "@fastify/cookie": "^11.0.1",
    "@fastify/jwt": "^10.2.0",
    "@fastify/rate-limit": "^10.1.1",
    "bcryptjs": "^2.4.3",
    "node-cron": "^4.6.0",
    "zod": "^3.23.8",
    "@modelcontextprotocol/sdk": "^1.12.0"
  }
}
